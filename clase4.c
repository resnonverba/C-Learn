#include <stdio.h>

int main()
{
   int hex, dec;
   printf("Hex: ");
   scanf("%x", &hex);
   printf("Dec: ");
   scanf("%d", &dec);
   printf("Hex en hexadecimal tiene el valor: %x\nHex en decimal tiene el valor de %d\nDec tiene el valor %d\n", hex, hex, dec);
}
